import { Container } from './styles';
import { Props } from './types';

export function {{ properCase name }}({ children, className }: Props) {
  return <Container className={className}>{children}</Container>;
}
